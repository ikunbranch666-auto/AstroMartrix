# 需求文档

## 1. 应用概述

### 1.1 应用名称
AstroMatrix（深空系外天体探索与空间中继指挥控制中枢）

### 1.2 应用描述
AstroMatrix 是一款高密度、纯静态前端深空探索与空间中继控制交互系统。系统采用 45° 斜切角机械工业美学设计，以单一节点定轨拖拽决策为核心交互，驱动树形面包屑层级、空间拓扑图结构、多轨遥测示波波形与全局有限状态机（FSM）的四维强联动，实现两阶段决策模型与动态降级调度。

## 2. 用户与使用场景

### 2.1 目标用户
- 深空探测指挥员与系统控制工程师
- 关注高阶视觉交互、图论编排与渲染架构的前端开发者与技术评估人员

### 2.2 核心使用场景
- **中继节点重轨调度**：用户在空间拓扑图中选中并拖拽处于活跃状态的中继节点，进入全息推演态，实时观察层级归属迁移、拓扑形变与时序遥测波形突变。
- **关键决策与固化/熔断**：推演达到警戒阈值后进入决策窗口期，用户通过键盘或界面操作完成不可逆锁定固化（COMMITTED），或因超时/主动取消触发自动熔断回弹协议（AUTO_ABORT）。

## 3. 页面结构与功能说明

### 3.1 页面结构

```
AstroMatrix 控制中枢
├── 顶部控制栏 (Top Bar)
│   ├── 梯形咬合面包屑 (Interlocking Breadcrumb)
│   └── 全局状态与热负荷指示器 (System Status & Heat Bar)
├── 核心交互区 (Central Viewport)
│   └── 空间中继拓扑画布 (Topology Canvas)
└── 遥测监控区 (Telemetry Viewport)
    └── 双轨遥测示波器 (Telemetry Scope)
```

### 3.2 核心功能说明

#### 3.2.1 顶部控制栏
- **45° 梯形咬合面包屑**：展示当前选定节点在深空层级中的空间路径（如：主星系 > 扇区 > 节点），在拖拽过程中实时反映层级归属的动态迁移。
- **状态与热负荷指示**：实时呈现当前决策阶段（STABLE_IDLE、STAGE1_SIMULATION、STAGE2_DECISION、COMMITTED、AUTO_ABORT）以及系统热负荷指数（0.0 至 1.0）。

#### 3.2.2 空间中继拓扑画布
- **拓扑节点与连线渲染**：渲染深空网络中的中继节点与关联边线，支持节点状态（空闲、选中、推演中、固化锁定）的差异化视觉表达。
- **物理定轨拖拽**：支持鼠标及指针拖拽活跃节点。拖拽时生成虚拟推演目标锚点与连接预览。
- **阻尼回弹机制**：在主动撤回（Esc）或超时熔断时，节点在1.2秒内通过阻尼物理动效平滑回弹至初始原位。
- **画质自适应（QoS）**：根据运行帧率自动在画质优先、平衡模式、性能优先三档间平滑切换渲染质量（如调整阴影、粒子数）。

#### 3.2.3 双轨遥测示波器
- **多轨波形实时绘制**：展示两路空间遥测时序波形，具备余辉渲染质感。
- **时序数据混合驱动**：稳态下展示标准周期遥测数据；节点拖拽时触发时序数据突变，支持波形数据的动态插值混合呈现。

#### 3.2.4 键盘与无障碍控制矩阵
- **全键盘操作链路**：支持方向键切换选定节点，Enter 键锚定与最终确认，Esc 键撤销推演。
- **实时状态语音播报**：集成无障碍实时区域，在状态流转与告警时输出语音/屏幕阅读器提示。

## 4. 业务规则与核心逻辑

### 4.1 两阶段决策状态机模型
1. **初始稳态 (STABLE_IDLE)**：系统处于待命状态，热负荷维持基线（~0.1）。用户可通过点击或键盘选择节点。
2. **阶段一：全息推演态 (STAGE1_SIMULATION)**：
   - 触发条件：用户开始拖拽活跃节点。
   - 持续限制：最大 20 秒。
   - 状态表现：拓扑图呈现虚拟虚线连线，热负荷指数自 0.1 线性爬升至 0.85，示波器波形发生频率偏移。
   - 操作权限：用户可随时按下 Esc 或取消拖拽，系统立即回弹至 STABLE_IDLE。
3. **阶段二：决策窗口期 (STAGE2_DECISION)**：
   - 触发条件：推演持续 20 秒，或用户松手时位移达到画布宽度 15%。
   - 持续限制：倒计时 10 秒。
   - 状态表现：热负荷指数达到警戒阈值（0.85 至 1.0），界面触发琥珀金报警闪烁，提示确认锁定。
   - 操作权限：用户按下 Enter 进行确认固化；按下 Esc 触发放弃；倒计时结束仍无操作则触发自动熔断。
4. **终局状态**：
   - **固化完成 (COMMITTED)**：拓扑结构永久更新，节点属性固化，系统热负荷平滑冷却回稳态。
   - **自动熔断/中止 (AUTO_ABORT)**：节点触发 1.2 秒弹性回弹，拓扑恢复原状，状态归零。

### 4.2 视觉与切角规范
- 采用 45° 与 135° 斜切倒角（Chamfer）构建面板和组件外壳，严格杜绝传统圆角。
- 视觉层级采用深黑底色（#06080B）搭配面板底色（#0D1117）与悬浮色（#161B22）；主数据色为青色（#00F0FF），警示色为琥珀金（#F59E0B），紧急色为品红（#FF0055）。

## 5. 异常与边界情况

| 序号 | 触发场景 | 系统处理逻辑 |
| :--- | :--- | :--- |
| 1 | 外部波形二进制资源加载失败 | 自动降级为内置纯数学公式生成波形，确保示波器正常运转 |
| 2 | 用户在阶段二窗口期（10秒）内未作任何输入 | 触发 AUTO_ABORT 熔断保护协议，节点自动回弹至初始坐标 |
| 3 | 系统运行帧率持续低于 45 FPS（超过 1 秒） | QoS 引擎自动降档，关闭高耗能阴影与背景滤镜，缩减波形采样点 |
| 4 | 系统运行帧率回升至 58 FPS 以上（持续 3 秒） | QoS 引擎自动升档恢复高精度渲染与粒子特效 |
| 5 | 高分辨率/Retina 屏幕接入 | 自动识别设备像素比（最高限制 2x），动态重设画布变换矩阵防止模糊 |

## 6. 验收标准

1. 用户在拓扑图中拖拽任意节点时，面包屑、拓扑连接、示波器波形和热负荷指数必须在同一交互帧内产生联动变化。
2. 拖拽节点超过设定阈值后，系统能准确从全息推演态进入决策窗口期，并展示 10 秒倒计时与告警视觉效果。
3. 在决策窗口期内按下 Enter 键，系统能完成定轨固化并将状态迁移至 COMMITTED，拓扑结构持久更新。
4. 在推演态或决策窗口期按下 Esc 键，或决策超时 10 秒后，节点能执行 1.2 秒阻尼平滑回弹并恢复初始状态。
5. 在键盘导航模式下，用户仅使用方向键、Enter 键和 Esc 键即可无障碍完成节点选择、推演与固化闭环。

## 7. 本期不实现功能

1. 真实后端服务端通信与用户鉴权登录体系
2. 3D WebGL / WebGPU 场景渲染引擎集成
3. 多人协同实时推演与冲突合并机制
4. 自定义节点拓扑导出为外部文件（如 JSON/XML）能力